@model ICollection<BeerApp.Web.ViewModels.Place.PlaceResponseViewModel>

@{
    ViewBag.Title = "All";
}

<h2>All Places</h2>
<div class="row">
    <div class="col-md-6">
        <h5><a href="~/Place/Add" class="btn btn-success">Add new PLACE!</a></h5>
    </div>
</div>

@foreach (var place in Model)
{
    @Html.Partial("_PlacePartial", place)
}